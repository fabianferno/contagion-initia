# Use a flat, hoisted node_modules (like npm/bun). The Initia SDK pulls
# cosmjs, whose internal deep imports (e.g. @cosmjs/amino/build/signdoc.js)
# break under pnpm's default strict symlinked layout but resolve fine when
# hoisted. Required for `vite build` and the server to resolve deps.
node-linker=hoisted
